PROGRAM TASKWAIT

INTEGER I
REAL PSUM(100), TSUM
PSUM=0; TSUM=0

!$OMP PARALLEL SHARED(I)
    DO I=1,100
       PSUM(I)= I
    ENDDO
    TSUM=SUM(PSUM)
!$OMP END PARALLEL

    PRINT*, TSUM

ENDPROGRAM
